# خطة تحسين واجهة المستخدم في Nabd

## الملخص التنفيذي

واجهة Nabd الحالية تمتلك أساسًا صحيحًا: الأحداث هي مصدر الحقيقة، وواجهة `Chat` وواجهة `Feed` تقرآن من السجل نفسه، كما أن صلاحيات الأدوات وحالات التشغيل ممثلة في أحداث مستقلة. لذلك لا ينبغي أن يبدأ التحسين بإعادة بناء الواجهة أو إضافة طبقة حالة منفصلة. المسار الأقل مخاطرة هو بناء **نظام عرض موحد** فوق الأحداث الحالية، ثم تحسين الإدخال، حالات التشغيل، الإذن، عرض الأدوات، والاسترداد.

الأولوية الأولى هي جعل الواجهة تجيب فورًا عن خمسة أسئلة:

1. هل Nabd ينتظر إدخال المستخدم أم ينفذ دورًا؟
2. ما الأداة التي تعمل الآن؟
3. هل يحتاج التنفيذ إلى إذن؟
4. هل حدث خطأ، وهل يمكن للمستخدم متابعة الجلسة؟
5. ما الذي تغيّر في الملفات، وما الذي يمكن التراجع عنه؟

الهدف ليس زيادة المعلومات المعروضة، بل **تقليل الغموض مع الحفاظ على قابلية التدقيق**.

## مبادئ التصميم

| المبدأ | التطبيق في Nabd |
|---|---|
| السجل هو مصدر الحقيقة | لا تُنشئ الواجهة حالة تشغيل مستقلة تناقض `agent.Event`. |
| العرض الحي وإعادة العرض متطابقان | كل مكوّن عرض جديد يجب أن يعمل مع الأحداث الحية و`--replay`. |
| الحالة قبل التفاصيل | أظهر الحالة والقرار أولًا، ثم التفاصيل عند التوسيع. |
| الأمان قبل الراحة | لا تعرض مفاتيح أو أجسام أخطاء خام، ولا تجعل الإذن غامضًا. |
| الهاتف أولًا | اختصر العروض على عرض ضيق، مع دعم التوسيع التدريجي. |
| قابلية الرجوع | كل إجراء خطر يجب أن يوضح أثره وطريقة التراجع عنه. |
| نص قابل للنسخ | لا تعتمد على الرموز اللونية وحدها، واجعل الرسائل مفهومة دون ANSI. |

## الوضع الحالي والمشكلات المستهدفة

### تجربة `Chat`

واجهة `Chat` الحالية تعتمد على سطر إدخال واحد، وتعرض معظم التاريخ في scrollback الطرفية. هذا يحافظ على البساطة، لكنه يجعل المستخدم يفتقد موضعه في الجلسة عندما تتعدد الأدوات أو تطول الإجابة. كما أن الشريط السفلي يعرض نصوصًا مثل `working` و`canceling`، لكنه لا يملك نموذجًا موحدًا لحالة الدور الحالي.

مشكلة أخرى هي أن أوامر `/undo` و`/rewind` و`/compact` تظهر كأوامر نصية داخلية، ولا توضح دائمًا الفرق بين تأثيرها على سجل المحادثة وتأثيرها على الملفات.

### تجربة `Feed`

واجهة `Feed` تقدم بطاقات للمستخدم والمساعد والأدوات والأذونات والأخطاء. هذا اتجاه جيد، لكن كثافة التفاصيل تختلف بين أنواع البطاقات، ولا توجد بعد لوحة حالة ثابتة تجمع معلومات الدور الحالي، عدد الأدوات، زمن التنفيذ، وحالة الاستهلاك.

عرض `read_file` يملك مسارًا خاصًا، لكن الانتقال بين الملخص والخرج الكامل يحتاج إلى إشارة أوضح إلى أن المحتوى مقتطع أو قابل للمتابعة عبر `offset`.

### الأذونات والأخطاء

حالة الإذن تتطلب مفاتيح `y`, `a`, و`n`. هذا مناسب للمستخدم الخبير، لكنه يحتاج إلى عرض أوضح للأثر: اسم الأداة، المسار أو الأمر، نوع العملية، وما إذا كان السماح لمرة واحدة أو للجلسة.

حالات الأخطاء الحالية تحمي من التسريب وتختصر الرسائل، لكن المستخدم يحتاج إلى فصل واضح بين:

- فشل قابل لإعادة المحاولة.
- فشل ناتج عن صلاحية أو إعداد.
- فشل في حفظ السجل.
- إيقاف يدوي.
- بلوغ حد الأدوار أو الإنفاق.

## البنية المقترحة

### طبقة حالة عرض موحدة

أضف حزمة صغيرة في `internal/presentation` أو وسّعها لتنتج نموذج عرض موحدًا من الأحداث. يجب أن تكون طبقة العرض reducer مزدوجة: `Apply` للتحديث الحي بتكلفة صغيرة، و`Project` لإعادة البناء من journal. يجب أن يثبت اختبار أن النتيجتين متطابقتان.

```go
type SessionPhase string

const (
    PhaseIdle       SessionPhase = "idle"
    PhaseThinking   SessionPhase = "thinking"
    PhaseTool       SessionPhase = "tool"
    PhasePermission SessionPhase = "permission"
    PhaseCompacting SessionPhase = "compacting"
    PhaseError      SessionPhase = "error"
    PhaseEnded      SessionPhase = "ended"
)

type SessionStatus struct {
    Phase            SessionPhase
    ActiveTools      []ActiveTool
    Turn             int
    TurnLimit        int
    Usage            UsageStatus
    CanCancel        bool
    CanRetry         bool
    LastError        *PresentedError
}

type ActiveTool struct {
    CallID    string
    Name      string
    Summary   string
    StartedAt time.Time
}

type UsageStatus struct {
    PromptTokens     int
    CompletionTokens int
    SpendUsed        *int64
    SpendLimit       *int64
    Complete         bool
}

type ErrorCode string

const (
    ErrCodeProviderAuth ErrorCode = "provider_auth"
    ErrCodePersist      ErrorCode = "persist"
    ErrCodeBudget       ErrorCode = "budget"
    ErrCodeCanceled     ErrorCode = "canceled"
    ErrCodeUnknown      ErrorCode = "unknown"
)

type PresentedError struct {
    Code      ErrorCode
    Message   string
    Retryable bool
}
```

يُشتق هذا النموذج من `agent.Live(events)`، ولا يُحفظ كحالة جديدة في journal. إذا لم تتوفر بيانات الإنفاق، تعرض الواجهة «الإنفاق: غير متاح» بدل عرض الصفر كأنه قيمة حقيقية.

يجب أن يعتمد ربط الأدوات على `CallID` لا على اسم الأداة. حتى إذا كان التنفيذ الحالي تسلسليًا، فإن `CallID` يحافظ على صحة العرض عند وجود أحداث متداخلة أو سجلات قديمة.

تُرتب أولوية الحالة عند التعارض كالتالي: خطأ الحفظ، ثم طلب الإذن، ثم الإلغاء، ثم الأداة، ثم الضغط، ثم التفكير، ثم الخمول، ثم الانتهاء. يبقى `Phase` منفصلًا عن `LastError`؛ فالخطأ التاريخي لا يعني بالضرورة أن الجلسة ما زالت في حالة خطأ.

### مكوّنات العرض

قسّم واجهة `Feed` إلى مكونات دلالية ثابتة:

1. `Header`: اسم المشروع، المزود/النموذج، ومرحلة الجلسة.
2. `Timeline`: بطاقات المستخدم والمساعد والأداة.
3. `StatusBar`: الحالة الحالية، الإلغاء، وحدود الإنفاق.
4. `PermissionCard`: طلب الإذن مع أثره ومفاتيح القرار.
5. `ErrorCard`: نوع الخطأ، الرسالة المختصرة، والإجراء التالي.
6. `Composer`: الإدخال، تاريخ الأوامر، والإكمال.

في `Chat` التقليدية، تُعرض المكونات نفسها بصيغة نصية مضغوطة بدل إعادة تعريفها من الصفر.

## المرحلة صفر: عقد المتانة قبل الواجهة

قبل بناء `SessionStatus` أو تغيير الشكل، يجب تثبيت صحة الحالة التي ستعرضها الواجهة:

1. إرجاع أخطاء `emitLocked` من `Compact` وأخطاء `emitAt` من `Rewind`.
2. منع تحديث الذاكرة قبل نجاح sink/journal.
3. إضافة أخطاء typed مثل `ErrCodePersist` بدل تحليل نص الخطأ في طبقة العرض.
4. إضافة اختبارات sink فاشل تثبت أن العملية لا تعرض نجاحًا زائفًا.
5. اختبار parity بين العرض الحي وإعادة العرض بعد فشل الحفظ.

لا يجوز أن تعرض الواجهة نجاح `compact` أو `rewind` قبل تأكيد حفظ الحدث. إذا فشل الحفظ، تبقى الجلسة في حالة قابلة للتشخيص وتظهر للمستخدم رسالة حفظ واضحة مع مسار journal.

## المرحلة الأولى: وضوح الحالة والإدخال

### الهدف

تقليل الغموض أثناء التنفيذ دون تغيير عقد الأحداث أو آلية تشغيل الأدوات.

### التغييرات

أضف شريط حالة ثابتًا في `Feed` ونسخة نصية مضغوطة في `Chat` تعرض:

```text
● جاهز · claude-sonnet-5 · السياق 32% · الإنفاق 4.2k/20k
```

أثناء التنفيذ:

```text
◌ يفكر · الدور 3/40 · ctrl+c للإيقاف
```

أثناء الأداة:

```text
▶ Read · README.md · 1.2s · مقتطع، التالي offset=170
```

أثناء الإذن:

```text
! يحتاج إذنًا · bash · سيشغّل: go test ./...
y مرة واحدة · a للجلسة · n رفض · Esc إلغاء
```

أضف عدادًا للدور الحالي إلى نموذج العرض. إذا لم يكن الرقم محفوظًا في الحدث، يُحسب من الأحداث الحية ولا يُعرض كحقيقة مستقلة.

### تحسين الإدخال

أضف إلى `Chat` و`Feed`:

- `Ctrl+U` لمسح السطر، وهو موجود في `Chat` ويجب توحيده في `Feed`.
- `Ctrl+W` لحذف الكلمة السابقة.
- `Up/Down` لتاريخ آخر 20 إدخالًا، دون حفظ النصوص الحساسة في journal.
- دعم لصق متعدد الأسطر مع تطبيع آمن قبل الإرسال.
- عرض طول الإدخال عند تجاوزه حدًا عمليًا.
- منع إرسال إدخال جديد أثناء الإذن مع إبقاء النص محفوظًا.

عقد اللصق متعدد الأسطر يجب أن يكون صريحًا: الحفاظ على الأسطر والمسافات البادئة، تحويل `CRLF` إلى `LF` فقط، عدم تفسير النص الملصوق كأمر slash تلقائيًا، فرض حد للحجم، ودعم bracketed paste. لا يُرسل النص تلقائيًا لمجرد احتوائه على newline. تنظيف control sequences يخص العرض فقط ولا يغير المحتوى المنطقي للرسالة.

مسودة الإدخال غير المرسلة تبقى في الذاكرة ولا تُكتب إلى journal. أما تاريخ الرسائل المرسلة فيُشتق من أحداث `UserMsg` ولا تُنشأ له نسخة ثانية. يجب ألا تدخل مفاتيح الإذن `y/a/n` في تاريخ الرسائل، ويُمسح تاريخ المسودات عند الانتقال إلى جلسة أو مشروع آخر.

### معايير القبول

- يعرف المستخدم مرحلة الجلسة خلال أقل من ثانية من تغير الحدث.
- لا تختفي الرسالة التي كتبها المستخدم عند رفض الإرسال أثناء التشغيل.
- يعمل الإدخال نفسه في `Chat` و`Feed`.
- لا تُكتب مسودة الإدخال أو تاريخ الإدخال إلى `session.jsonl`.

## المرحلة الثانية: إعادة تصميم بطاقات الأدوات

### الهدف

جعل الأداة قابلة للفهم من السطر الأول، وقابلة للتفصيل عند الطلب.

### الشكل المضغوط

كل بطاقة أداة تعرض بالترتيب:

```text
✓ Read  README.md  840ms  170/400 lines
```

أو:

```text
✗ Bash  go test ./...  2.4s  exit 1
```

أو:

```text
◌ Edit  internal/agent/loop.go  running
```

### التوسيع التدريجي

عند التوسيع، اعرض:

1. الوسائط المنظفة.
2. الحالة والمدة.
3. خرج الأداة مع حد واضح.
4. سبب الفشل إن وجد.
5. رابط أو أمر منطقي للمتابعة، مثل `read_file offset=170`.

لا تعرض الخرج الكامل تلقائيًا للأدوات الكبيرة. استخدم مستويات:

- ملخص.
- أول وآخر جزء.
- العرض الكامل عند الطلب.

### `read_file`

أضف شارة واضحة:

```text
READ 132–169 · truncated · next offset 170
```

وعند عرض نتيجة غير مكتملة، لا توحِ بأنها ملف كامل. يجب أن يبقى هذا واضحًا في `Chat` النصية أيضًا.

### معايير القبول

- يمكن تمييز نجاح الأداة وفشلها دون الاعتماد على اللون.
- لا يتجاوز أي سطر عرض عرض الطرفية.
- تُعرض حالة القص والـ`next_offset` في كل من العرض الحي وإعادة العرض.
- لا يؤدي توسيع بطاقة ضخمة إلى تجميد واجهة المستخدم.

## المرحلة الثالثة: تجربة الأذونات الآمنة

### الهدف

تحويل طلب الإذن من اختصار لوحة مفاتيح غامض إلى قرار مفهوم وقابل للمراجعة.

### بطاقة الإذن

اعرض:

```text
PERMISSION REQUIRED
Tool: bash
Command: go test ./...
Scope: this command only

[y] Allow once   [a] Allow session   [n] Deny   [Esc] Cancel
```

للأدوات التي لا تسمح بـ`AllowSession`، لا تعرض الخيار أصلًا. يجب أن يكون العرض مشتقًا من سياسة الأداة، لا من اسمها فقط.

لعمليات الكتابة، أظهر:

- الملف المستهدف.
- نوع العملية.
- حجم التغيير إن توفر.
- هل يوجد سجل undo متاح.

### حالات الإذن

استخدم حالات مستقلة:

- `waiting`: ينتظر قرار المستخدم.
- `allowed_once`: نُفذ مرة واحدة.
- `allowed_session`: السماح محفوظ للجلسة.
- `denied`: رفض مع استمرار الحوار.
- `canceled`: ألغاه المستخدم.
- `expired`: انتهت الجلسة قبل القرار.

### معايير القبول

- لا يمكن تفسير السماح للجلسة على أنه سماح دائم.
- لا يمكن استخدام `a` لأداة لا تدعم صلاحية الجلسة.
- بعد الرفض، يبقى بإمكان النموذج متابعة الحوار.
- لا تظهر مفاتيح أو أسرار في نص طلب الإذن.

## المرحلة الرابعة: الأخطاء والاسترداد

### تصنيف موحد

استخدم `ErrorCode` و`PresentedError` المعرّفين في نموذج الحالة، وأرسل الرمز إلى العرض عبر metadata في الحدث. في السجلات القديمة التي لا تحتوي على code تستخدم الواجهة `ErrCodeUnknown`، ولا تحاول تخمين النوع من نص الرسالة أو رقم HTTP. ويظهر لكل خطأ إجراء تالٍ واحد على الأقل:

| الحالة | الرسالة | الإجراء |
|---|---|---|
| مزود مؤقت | تعذر الوصول إلى المزود | إعادة المحاولة |
| 401/403 | المسار غير متاح أو المفتاح مرفوض | فحص الإعدادات |
| sink/journal | لم يُحفظ الحدث | إيقاف آمن وإظهار المسار |
| spend budget | بلغ التنفيذ سقف الإنفاق | بدء جلسة جديدة أو رفع الحد |
| max turns | لم تستقر الجلسة | متابعة مختصرة أو إعادة صياغة |
| cancel | أوقف المستخدم الدور | متابعة برسالة جديدة |

لا تجعل زر أو اختصار إعادة المحاولة يعيد تنفيذ أداة خطرة تلقائيًا. إعادة المحاولة تخص طلب المزود أو الدور، وتحتاج إلى قرار جديد للأداة عند الحاجة.

## المرحلة الخامسة: تحسين الأوامر والتنقل

### مركز المساعدة

يجب أن يعرض `/help` جدولًا قابلًا للقراءة على العرض الضيق:

```text
/ctx       حالة السياق والإنفاق
/compact   ضغط التاريخ، لا يحذف الملف
/rewind n  قطع آخر n أدوار، لا يتراجع عن تعديل الملفات
/undo n    التراجع عن تعديلات الملفات المسجلة
/edits     عرض التعديلات القابلة للتراجع
```

أضف إكمالًا للأوامر عند كتابة `/`، مع عرض الوصف المختصر. إذا كان الأمر يحتاج رقمًا، اعرض الصيغة الصحيحة بدل رسالة خطأ عامة.

### التنقل في Feed

أضف:

- `j/k` أو الأسهم للتنقل بين البطاقات.
- `Enter` لتوسيع البطاقة.
- `Esc` للعودة إلى الملخص.
- `g/G` للبداية والنهاية.
- `n` للانتقال إلى الخطأ التالي.
- `p` للانتقال إلى آخر طلب إذن.

يجب أن تظهر هذه المفاتيح في تلميح صغير قابل للإخفاء، لا أن تستهلك مساحة ثابتة كبيرة.

## المرحلة السادسة: الهوية البصرية وإتاحة الوصول

### نظام ألوان دلالي

لا تربط المعنى باسم لون واحد. عرّف دلالات:

| الدلالة | اللون المقترح | بديل نصي |
|---|---|---|
| نجاح | أخضر هادئ | `✓ success` |
| تنفيذ | أزرق أو سماوي | `◌ running` |
| تحذير/إذن | أصفر | `! permission` |
| خطأ | أحمر | `✗ error` |
| معلومات | رمادي/أبيض | `· info` |

اختبر الواجهة دون ألوان عبر `NO_COLOR` أو إعداد مكافئ. يجب ألا تعتمد الحالة على اللون وحده.

### العربية واللغات المختلطة

بما أن Nabd يستعمل العربية والإنجليزية وأسماء الملفات والأوامر، يجب:

- عدم قلب مسار الملف أو الأمر تلقائيًا مع النص العربي.
- عرض الأوامر في مساحة LTR واضحة.
- اختبار أسماء الملفات العربية والمختلطة.
- حماية قياس العرض من ANSI وUnicode combining marks.
- إبقاء الاختصارات مثل `ctrl+c` و`offset=170` كما هي.

### أحجام الطرفية

يجب اعتماد ثلاثة أوضاع:

| العرض | السلوك |
|---|---|
| أقل من 40 | ملخص سطر واحد، إخفاء metadata الثانوية |
| 40–79 | بطاقات مضغوطة مع metadata أساسية |
| 80 فأكثر | تفاصيل إضافية وتلميحات التنقل |

لا تستخدم قيمة width ثابتة واحدة لكل المكونات. يجب أن يملك كل مكوّن دالة قياس واختبار snapshot.

## المرحلة السابعة: الأداء والاستقرار

### مخاطر الأداء

تجميع `TextDelta` في `Chat` مناسب للعرض النصي، لكن Feed قد يضم عددًا كبيرًا من البطاقات. ينبغي:

- استخدام window للبطاقات المرئية فقط.
- عدم إعادة تنسيق كل التاريخ عند وصول `TextDelta` جديد.
- تخزين نتائج layout مؤقتًا حسب `(event range, width, expanded state)`.
- عرض summary أولًا، ثم كل المحتوى المحفوظ عند التوسيع.
- إظهار شارة إذا كان الخرج مقتطعًا عند التنفيذ أو التخزين؛ لا تعرض زر `Full output` إذا كانت بقية النتيجة غير موجودة.
- جعل `read_file` يعرض `next_offset`، وعدم الإيحاء بأن خرج `bash` الكامل قابل للاسترداد إذا لم يُحفظ.
- قياس زمن `Update` و`View` في اختبار حمل من 1,000 و10,000 حدث.

### معايير الأداء

- إنشاء baseline على جهاز Termux مرجعي قبل تثبيت أي هدف رقمي.
- منع regression نسبي: لا يزيد الزمن أكثر من 20% ولا تزيد allocations أكثر من 15% عن baseline.
- عدم زيادة الذاكرة مع خرج أداة ضخمة عند عدم توسيعها.
- اعتماد سياسة `journal أولًا → UI notification → catch-up من journal عند اكتشاف فجوة Seq`؛ لا تحاول الواجهة ضمان lossless delivery عبر رفع حجم القناة فقط.

## خطة التنفيذ الزمنية

| المرحلة | النطاق | المخرجات |
|---|---|---|
| Sprint 0 | عقد المتانة | أخطاء typed، فشل sink، live/replay parity بعد فشل الحفظ |
| Sprint 1 | Presentation projector | `SessionView`, reducer incremental، projection من التاريخ |
| Sprint 2 | الحالة والأذونات والأخطاء | StatusBar وPermissionCard وErrorCard والعرض الضيق |
| Sprint 3 | بطاقات الأدوات | pairing بالـCallID، truncation، next offset، توسيع bounded |
| Sprint 4 | الإدخال والأوامر | تاريخ محلي، bracketed paste، slash completion، help |
| Sprint 5 | التنقل وإتاحة الوصول | مفاتيح التنقل، أوضاع العرض الضيق، NO_COLOR |
| Sprint 6 | الأداء وإعادة العرض | baseline، profiling، catch-up، cache عند الحاجة |

يجب تنفيذ كل Sprint في Pull Request مستقل، مع إبقاء عقد `agent.Event` متوافقًا قدر الإمكان.

## ترتيب الأولويات

### P0 — قبل أي تحسين تجميلي

1. تثبيت عقد المتانة وأخطاء الحفظ typed.
2. بناء projector incremental مع parity مقابل إعادة البناء من journal.
3. توحيد حالات الجلسة وعرضها في `Chat` و`Feed`.
4. إصلاح وضوح طلبات الإذن.
5. تمييز الأخطاء القابلة للاسترداد عن أخطاء الحفظ والميزانية.
6. إظهار حالة `read_file` المقتطع و`next_offset`.
7. اختبار العرض على عرض أقل من 40 عمودًا.

### P1 — تحسين تجربة الاستخدام اليومية

1. سجل الإدخال المحلي.
2. إكمال slash commands ومساعدة سياقية.
3. بطاقات أدوات قابلة للتوسيع.
4. تنقل لوحة المفاتيح داخل Feed.
5. شريط حالة الإنفاق والسياق.

### P2 — الصقل والأداء

1. نظام ألوان دلالي قابل للتعطيل.
2. دعم أفضل للنص العربي والمختلط.
3. تخزين layout مؤقتًا وvirtualization.
4. اختبارات snapshot لكل عرض وعرض طرفية.
5. لوحة مقاييس اختيارية للتشخيص.

## استراتيجية الاختبار

### اختبارات العقد

- لا يتجاوز عدد الصفوف المساحة المحجوزة.
- لا يتجاوز أي سطر `width`.
- لا يظهر ANSI عند تفعيل `NO_COLOR`.
- لا يظهر سر في أي بطاقة.
- يظهر `next_offset` عند وجود truncation.

### اختبارات الانتقال

- `idle → tool → permission → tool → idle`.
- فشل الحفظ.
- الإلغاء.
- parity بين العرض الحي وإعادة العرض.

### اختبارات وحدة

- تحويل كل نوع `agent.Event` إلى `SessionStatus`.
- انتقالات الحالة: idle → thinking → tool → permission → idle/error.
- قياس النصوص عند widths: 20، 39، 40، 79، 80، 120.
- تنقية الأوامر والمسارات والأخطاء.
- عدم ظهور الأسرار في أي بطاقة.

### اختبارات تكامل

- دور كامل مع أداة قراءة مقتطعة.
- رفض إذن ثم متابعة الدور.
- فشل journal أثناء `Compact` و`Rewind`.
- بلوغ spend budget.
- إعادة عرض journal نفسه ومقارنة الناتج مع العرض الحي.

### اختبارات الأداء

أنشئ baseline أولًا على جهاز Termux مرجعي وسجل `ns/op` و`B/op` و`allocs/op`. اجعل شرط المنع نسبيًا: لا يزيد الزمن عن 20% ولا تزيد allocations عن 15%. تبقى أرقام GitHub Runner وتقارير Termux معلوماتية ما لم يوجد جهاز ثابت في CI. لا تبدأ virtualization أو layout cache قبل إثبات عنق الزجاجة عبر profiling.

### اختبارات snapshot

يجب حفظ snapshots صغيرة، لا جلسات ضخمة، للحالات التالية:

- رسالة مستخدم عربية.
- إجابة مساعد متعددة الأسطر.
- أداة `read_file` مقتطعة.
- أداة `bash` فاشلة.
- طلب إذن bash.
- خطأ مزود مع fallback.
- حالة الإنفاق والـcontext.
- طرفية ضيقة جدًا.

## معايير نجاح المنتج

تُعتبر الخطة ناجحة عندما يستطيع مستخدم جديد، دون قراءة README، أن يجيب من الواجهة عن حالة الجلسة والأداة والقرار المطلوب والخطوة التالية. ويجب أن يستطيع المستخدم الخبير تنفيذ الأوامر نفسها بعدد ضغطات أقل، مع بقاء السجل قابلًا لإعادة العرض والتدقيق.

المؤشرات المقترحة بعد التطبيق:

- انخفاض رسائل الدعم المتعلقة بـ«هل ما زال Nabd يعمل؟».
- انخفاض محاولات إعادة تشغيل الأداة بسبب سوء فهم الحالة.
- انخفاض أخطاء الإذن الناتجة عن اختيار خاطئ.
- عدم وجود فرق دلالي بين live view وreplay view.
- بقاء زمن إعادة الرسم ضمن الحدود المحددة في جلسات طويلة.

قبل اعتماد الخطة كخطة تنفيذ، يجب البدء بـSprint 0، واعتماد `ErrorCode` typed، وتطبيق سياسة journal-first مع catch-up عند فقد أحداث UI. لا تُعرض أرقام الإنفاق عندما تكون غير معروفة، ولا يُستخدم تحليل النص لتصنيف الأخطاء، ولا تُوعد الواجهة بخرج كامل لم يُحفظ أصلًا.

## مراجع

[1]: https://github.com/amiraq1/Nabd "Nabd source repository"
[2]: https://pkg.go.dev/github.com/charmbracelet/bubbletea "Bubble Tea package documentation"
[3]: https://pkg.go.dev/github.com/charmbracelet/lipgloss "Lip Gloss package documentation"
